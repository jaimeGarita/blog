---
const { title, description, imageUrl, linkUrl } = Astro.props;
---

<div class="border-2 border-gray-300 p-4 shadow-md rounded-lg flex flex-col items-center">
    <img src={imageUrl} alt={title} class="w-full h-40 object-cover rounded-t-lg mb-4 cursor-pointer" onclick="openModal('{imageUrl}')" />
    <h2 class="text-xl font-semibold mb-2">{title}</h2>
    <p class="text-gray-700 mb-4 text-center">
        {description}
    </p>
    <a href={linkUrl} class="text-blue-500 hover:text-blue-700 font-semibold">
        Ver más
    </a>
</div>
